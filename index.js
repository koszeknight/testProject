/**
 * @format
 */

import {AppRegistry} from 'react-native';
import LoginApp from './src/FirstPage';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => LoginApp);
